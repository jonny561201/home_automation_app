import {StyleSheet} from 'react-native';

export default StyleSheet.create({

    pageContainer: {
        paddingTop: 30,
    },

    settingsHeader: {
        fontSize: 26,
        fontWeight: 'bold',
    },

    settingsBody: {
        flexDirection: 'column',
        marginLeft: 10,
    },

    settingsLabelText: {
        paddingTop: 12,
        fontSize: 16,
    },

    settingsWrapper: {
        margin: 1,
        flexDirection: 'column',
    },

    settingsRow: {
        flexDirection: 'row',
        marginLeft: 20,
    },
});