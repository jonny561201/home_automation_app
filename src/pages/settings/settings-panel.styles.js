import {StyleSheet} from 'react-native';

export default StyleSheet.create({

    settingsHeader: {
        fontSize: 26,
        fontWeight: 'bold',
        paddingTop: 10,
        paddingBottom: 10,
    },

    measureUnit: {
        fontSize: 16,
    },

    tempUnit: {
        fontSize: 16,
    },

    tempCity: {
        fontSize: 16,
    },

    settingsRow: {
        paddingTop: 12,
        paddingBottom: 12,
        flexDirection: 'row',
        marginLeft: 20,
    },

    buttonRow: {
        marginBottom: 20,
    },
});